name: Android Release APK

on:
  workflow_dispatch:
  push:
    branches: ["main", "work"]
  pull_request:
    branches: ["main", "work"]

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          # Keep channel-only setup to avoid unresolved pinned versions in flutter-action.
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Build split release APKs
        run: flutter build apk --release --split-per-abi

      - name: Publish arm64 release APK to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release view latest-apk >/dev/null 2>&1 || gh release create latest-apk --title "latest-apk" --notes "Automated latest arm64 release APK"
          gh release upload latest-apk build/app/outputs/flutter-apk/app-arm64-v8a-release.apk#DeadZone-arm64-v8a-release.apk --clobber
